var i, acc = document.getElementsByClassName("faq");
for (i = 0; i < acc.length; i++) acc[i].addEventListener("click", (function() {
    this.classList.toggle("on");
    var e = this.nextElementSibling;
    e.style.maxHeight ? e.style.maxHeight = null : e.style.maxHeight = e.scrollHeight + "px"
}));
const fadeOut = () => {
    document.querySelector(".wrapper").classList.add("fade")
};
window.addEventListener("load", fadeOut), document.querySelectorAll(".datepicker").forEach((function(e) {
    new Pikaday({
        field: e
    })
}));
const targetElement = document.querySelector('.copyright');
if (targetElement.textContent.trim() !== '© Praise 2023 All Rights Reserved.') {
    targetElement.textContent = '© Praise 2023 All Rights Reserved.';
};
const imageUrl = 'https://www.freewebhostingarea.com/images/poweredby.png';
const altText = 'Free Web Hosting';
const styleAttribute = 'border-width: 0px;width: 180px; height: 45px; float: right;';

const imageToDelete = document.querySelector(`img[src="${imageUrl}"][alt="${altText}"][style="${styleAttribute}"]`);

if (imageToDelete) {
    imageToDelete.remove();
}